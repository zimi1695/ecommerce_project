# 换行符统一为 LF（仓库内），检出时按平台自动转换
* text=auto eol=lf

# Windows 批处理保持 CRLF
*.bat text eol=crlf
*.ps1 text eol=crlf

# 二进制文件不做转换
*.png binary
*.jpg binary
*.gif binary
*.ico binary
*.woff binary
*.woff2 binary
