# npm run dev 默认使用 Mock；npm run dev:api / npm run build 默认使用真实接口。
# 仅需覆盖默认值时复制为 .env.local；其中的 VITE_USE_MOCK 会覆盖命令默认值。
# VITE_USE_MOCK=false
VITE_API_BASE_URL=/api
API_PROXY_TARGET=http://127.0.0.1:8000
